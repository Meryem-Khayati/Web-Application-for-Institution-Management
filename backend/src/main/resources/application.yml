server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestionetablissement
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 100MB # Définissez ici la taille maximale autorisée pour les fichiers téléchargés
      max-request-size: 100MB # Définissez ici la taille maximale autorisée pour l'ensemble de la requête
  mail:
    host: smtp.gmail.com
    username: marykhayati72@gmail.com
    password: rtzsadsssecdesms
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
